bad_function <- function(){print("put everything on one line of function so styler won't like it - make it too long and generally not good practise")}

alsoMakeSomethingInThisFormat <- 'not snake format'

have_some_weird_indents <- data.frame("a" = 1,
                            "b" = 3,
                    "c" = 6,
                                "d" = 0)
